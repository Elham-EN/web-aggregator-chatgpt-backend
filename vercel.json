{
  "version": 2,
  "builds": [{ "src": "./index.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/api/(.*)", "dest": "/" },
    { "src": "/api/(.*)", "headers": { "Access-Control-Allow-Origin": "*" } }
  ]
}
